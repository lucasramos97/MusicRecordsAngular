import { MusicFeatPipe } from './music-feat.pipe';

describe('MusicFeatPipe', () => {
  it('create an instance', () => {
    const pipe = new MusicFeatPipe();
    expect(pipe).toBeTruthy();
  });
});
